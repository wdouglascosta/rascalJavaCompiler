program prog1;

var x, i, result: integer;

begin
 
    read(x);
    i := 1;
    if(x <= 100) then
    begin
        while(i <= x) do
          begin
              result := result + i;
              i := i + 1;
          end
    end
    write(result);
end.

