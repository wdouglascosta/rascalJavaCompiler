program procPassRef;
   var k: integer;

procedure proc1(n: integer, g: integer);
  var h:integer;
begin
  h := n + g;
  write(h);
end

begin
  k := 5;
  proc1(3, k);
  proc1(10, k);
  write(k);
end.
