program atribuicao;

  var x, y, a: integer;

begin
  if 3 < 4 then
  begin
    read(a);
    a := 10;
    y := a;
    x := 2 + 5 * 3;
    y := 3 * x;
    print(a + 2 * 3);
  end

  if (x + y < 10) then
  begin
    write(x + 1, y);
  end

  if (2 + b * a < y div a) then
  begin
    a := x + 3;
    print(a);
  end
end.

